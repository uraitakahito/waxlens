{
  "name": "@waxlens/daemon",
  "version": "0.0.0",
  "description": "Stateless HTTP/WS validation daemon — owns @waxlens/core, serves the tui and (future) browser clients.",
  "license": "Unlicense",
  "type": "module",
  "engines": {
    "node": ">=24.15.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "waxlens-daemon": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && node ../../scripts/gen-build-info.mjs daemon && tsc -p tsconfig.build.json",
    "postbuild": "chmod +x dist/cli.js",
    "typecheck": "node ../../scripts/gen-build-info.mjs daemon && tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check": "pnpm typecheck && pnpm lint && pnpm build && pnpm test"
  },
  "dependencies": {
    "@waxlens/contract": "workspace:*",
    "@waxlens/core": "workspace:*",
    "@waxlens/protocol": "workspace:*",
    "commander": "^15.0.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "@types/ws": "^8.5.12",
    "eslint": "^10.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
