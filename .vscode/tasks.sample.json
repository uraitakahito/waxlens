{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pnpm: build @wacz-validator/core",
      "type": "shell",
      "command": "pnpm --filter @wacz-validator/core build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$tsc"],
      "presentation": {
        "reveal": "silent",
        "clear": true,
        "close": true
      }
    },
    {
      "label": "pnpm: build @wacz-validator/tui",
      "type": "shell",
      "command": "pnpm --filter @wacz-validator/tui build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$tsc"],
      "presentation": {
        "reveal": "silent",
        "clear": true,
        "close": true
      }
    }
  ]
}
