{
  "//": [
    "Discussion on how to write comments in package.json:",
    "https://stackoverflow.com/questions/14221579/how-do-i-add-comments-to-package-json-for-npm-install"
  ],
  "name": "waxlens-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for waxlens — @waxlens/core (validator) and @waxlens/tui (Ink TUI).",
  "license": "Unlicense",
  "type": "module",
  "engines": {
    "node": ">=24.15.0"
  },
  "packageManager": "pnpm@11.20.0+sha512.9a6f330a95b66446ea088faf1521405a8a01f07fde7124cc9958dfed52d4bb436737e65b08f85f37b46fcba375092558ac51262b816844b22f63406ed166bfee",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:ui": "vitest --ui",
    "test:report": "vitest run --reporter=html",
    "lint": "pnpm -r lint",
    "check": "pnpm audit --audit-level=moderate && pnpm --filter @waxlens/contract check && pnpm --filter @waxlens/core check && pnpm --filter @waxlens/validate-cli check && pnpm --filter @waxlens/protocol check && pnpm --filter @waxlens/daemon check && pnpm --filter @waxlens/devtools check && pnpm --filter @waxlens/tui check",
    "site:dev": "npm --prefix docs-site run dev",
    "site:build": "npm --prefix docs-site run build",
    "site:preview": "npm --prefix docs-site run preview",
    "site:check": "npm run site:build && node scripts/check-doc-refs.mjs"
  },
  "//scripts": [
    "test:ui / test:report use the root vitest.config.ts, whose `projects` point at",
    "the per-package configs — so the UI shows core, daemon and tui in one run rather",
    "than needing three. `pnpm test` is unchanged and still goes through `pnpm -r test`.",
    "test:report writes html/ and cannot be opened over file:// — serve it, e.g.",
    "`npx vite preview --outDir html`."
  ],
  "//@types/node": [
    "Root-level eslint.config.js uses `// @ts-check` plus `import.meta.dirname`",
    "(Node 20.11+). The `dirname` field lives in @types/node's ImportMeta",
    "interface augmentation — without it the VSCode TS LSP reports `Property",
    "'dirname' does not exist on type 'ImportMeta'`. pnpm's default hoisting is",
    "per-workspace, so @types/node already declared in packages/*/package.json",
    "is NOT visible from the monorepo root; it must be a direct devDependency",
    "here so a symlink lands at <root>/node_modules/@types/node."
  ],
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.0",
    "@vitest/ui": "^4.1.11",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import-x": "^4.16.2",
    "typescript-eslint": "^8.68.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
